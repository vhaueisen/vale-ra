using TMPro;
using UnityEngine;

public class ToastLabel : MonoBehaviour
{
    //public GameObject label;
    public string ObjName;

    // void Start()
    // {
    //     label = Instantiate(labelPrefab, transform.position, Quaternion.identity, transform);
    //     label.transform.localPosition = offset;
    //     label.GetComponentInChildren<TextMeshPro>().text = ObjName;
    //     label.SetActive(false);
    // }

    // void Update()
    // {
    //     if (label.activeInHierarchy)
    //     {
    //         label.transform.LookAt(Camera.main.transform);
    //     }
    // }
}
